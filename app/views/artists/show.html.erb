<%= link_to "Voltar", event_artists_path, class: "back-link"%>


<div class="mx-auto" style="width: 900px;">
  <div class="d-flex justify-content-center">
    <h4><%= @artist.name %></h4>
  </div>
</div>

<p>Nacionalidade, year of birth / Country of residence, year of death</p>
<p>Biografia: link Encyclopedia ITAU</p>
<p>Obras expostas nessa edição</p>
<p><%= @event.edition %></p>
<div class="mx-auto" style="width: 900px;">
  <div class="d-flex justify-content-center">
  <table>
    <tbody>  
    <% @work.each do |work| %>
    <tr>
      <td><%= work.title %></td>
      <td><%= work.date %></td>
      <td><%= work.size %></td>
      <td><%= work.support %></td>
      <td><%= work.technique %></td>
      <% end %>
    </tr>
    </tbody>
  </table>
  </div>
</div>

<p>Circulação</p>




